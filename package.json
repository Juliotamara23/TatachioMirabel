{
  "name": "tatachio-mirabel-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test:web": "pnpm --filter frontend build && playwright test --config=scripts/qa/suites/web/playwright.config.ts"
  }
}
